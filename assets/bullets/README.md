# Sprites de Balas

Actualmente las balas se renderizan como texto de codigo (print('hello'), import pygame, etc.).

Si se desea imagen:
- code_bullet.png: 16x4 px, rectangulo horizontal verde brillante
- Se reemplazaria el render de texto por la imagen en Bullet.__init__()
